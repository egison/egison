-- P2 D3: a free capability binder in an explicit matcher scheme is rigid
-- while the body is checked.  `something` has intrinsic capability `none`;
-- it must not specialize the declared capability p to none.

def p2BogusCapability : Matcher p Integer := something
