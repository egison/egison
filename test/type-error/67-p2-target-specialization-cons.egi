-- P2: specializing a catch-all matcher's target to a collection must not
-- create collection constructor capability.

def p2CatchAll {a} : Matcher none a :=
  matcher
    | $ as something with
      | $tgt -> [tgt]

def bad := matchAll [1, 2] as p2CatchAll with
  | $x :: $xs -> x
