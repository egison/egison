-- TypePM match-site fail-closed boundary: the fresh-value expression has type
-- Any, but Any is not producer evidence for a structured pattern capability.

def anyCapabilityBypass :=
  matchAll [1, 2] as # with
    | $x :: $xs -> x
