--
-- Heterogeneous matcher collection.  `something : Matcher none b` and
-- `list integer : Matcher [none] [Integer]` have different capability
-- heads.  Unifying only their target types must not strengthen
-- `something`; the list literal is rejected.
--
-- Expected: Type error (capability mismatch: none vs [none])
--

def t := matchAll [something, list integer] as list something with
  | $m :: _ ->
    matchAll [1, 2] as m with
      | $x :: _ -> x
