-- R12 component checking must not skip lambda applications.  This expression
-- has the same complete inferred type `Matcher Any a` as mPoly itself.
--
-- Expected: Type error (`Any` capability at a list-headed hole)

inductive Box := Box [Integer]
inductive pattern Box := box [Integer]

def mPoly {a} : Matcher Any a :=
  matcher
    | $ as something with
      | $tgt -> [tgt]

def bad : Matcher Box Box :=
  matcher
    | box $ as (\m -> m) mPoly with
      | Box $xs -> [xs]
      | _ -> []
    | $ as something with
      | $tgt -> [tgt]
