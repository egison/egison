-- Target specialization is allowed, but ground capability Any does not unify
-- with ground capability Integer.  `eq` therefore cannot be annotated as a
-- matcher that supports Integer constructor patterns.

def myint : Matcher Integer Integer := eq
