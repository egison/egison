-- A no-capability matcher value cannot be bound at a structured matcher
-- type.  This cast would let cons patterns reach
-- `something`, which cannot decompose them (well-typed but stuck).

def m2 : Matcher [none] [Integer] := something
