-- A type annotation nested inside an explicitly quantified definition uses
-- the same rigid binder as the outer scheme.  Treating the inner `a` as a
-- fresh flexible variable would wrongly allow it to specialize to Integer.

def p2NestedAnnotationMustStayRigid {a} (x : a) : Integer :=
  let y := (1 : a)
   in 0
