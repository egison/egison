--
-- Tuple pattern at a no-capability matcher (paper Lemma "MS Progress",
-- tuple case).  The tuple pattern requires product capability, so
-- `something : Matcher none b` fails the capability check.
--
-- Expected: Type error (something vs a product-headed slot)
--

def t := matchAll (1, 2) as something with ($x, $y) -> x
