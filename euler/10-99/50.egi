(load "lib/math/prime-numbers.egi")

(define $n 1000000)

(io (each (compose show print) (map 1#(rac (filter 2#(prime? %2) (zip nats (while (lt? $ n) (cdr (scanl + 0 (drop %1 primes))))))) (between 0 100))))
