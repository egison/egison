;;
;; Pattern.egi
;;

;(define $n-cons
;  (lambda [$n $v]
;    (loop $l $i (between 1 n) <cons $`v_i l> _)))

;(define $comb
;  (lambda [$n $v]
;    (loop $l $i (between 1 n) <join _ <cons $`v_i l>> _)))

;(define $comb/duplicate
;  (lambda [$n $v]
;    (loop $l $i (between 1 n) <join _ (& <cons $`v_i _> l)> _)))
