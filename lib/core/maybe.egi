--
--
-- Maybe (Option)
--
--

inductive Maybe a :=
  | Nothing
  | Just a

def maybe {a} (m: Matcher a) : Matcher (Maybe a) :=
  matcher
    | nothing as () with
      | Nothing -> [()]
      | _ -> []
    | just $ as (m) with
      | Just $x -> [x]
      | _ -> []
    | $ as (something) with
      | $tgt -> [tgt]
