--
--
-- Maybe (Option)
--
--

inductive pattern Maybe a :=
  | nothing
  | just a

inductive Maybe a :=
  | Nothing
  | Just a

def maybe {a} (m: MatcherSlot p a) : Matcher (Maybe p) (Maybe a) :=
  matcher
    | nothing as () with
      | Nothing -> [()]
      | _ -> []
    | just $ as (m) with
      | Just $x -> [x]
      | _ -> []
    | $ as (something) with
      | $tgt -> [tgt]
