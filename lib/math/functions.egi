;;;;;
;;;;;
;;;;; Mathematical Functions
;;;;;
;;;;;

(define $sigmoid
  (lambda [$z]
    (/ 1 (+ 1 (exp (* -1 z))))))

(define $kronecker-delta
  (lambda [$i $j]
    (if (eq? i j)
      1
      0)))
